apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Preview environments are automatically added here by generate-preview.sh
# Each preview is a subdirectory with its own kustomization
# Example:
#   resources:
#     - namespace.yaml
#     - abc1234/
#     - def5678/
resources:
  - namespace.yaml
