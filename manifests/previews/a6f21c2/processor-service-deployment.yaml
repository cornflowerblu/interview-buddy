apiVersion: apps/v1
kind: Deployment
metadata:
  name: processor-service
  namespace: previews
  labels:
    app: processor-service
    preview: "a6f21c2"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: processor-service
      preview: "a6f21c2"
  template:
    metadata:
      labels:
        app: processor-service
        preview: "a6f21c2"
    spec:
      containers:
        - name: processor-service
          image: ibdevacrpuj79a.azurecr.io/processor-service:preview-a6f21c2
          ports:
            - containerPort: 3000
          envFrom:
            - secretRef:
                name: previews-nextjsportfolio-env
          env:
            - name: NODE_ENV
              value: "preview"
          resources:
            requests:
              memory: "128Mi"
              cpu: "50m"
            limits:
              memory: "256Mi"
              cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: processor-service
  namespace: previews
  labels:
    preview: "a6f21c2"
spec:
  selector:
    app: processor-service
    preview: "a6f21c2"
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
