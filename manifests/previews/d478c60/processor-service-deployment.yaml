apiVersion: apps/v1
kind: Deployment
metadata:
  name: processor-service
  namespace: previews
  labels:
    app: processor-service
    preview: "d478c60"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: processor-service
      preview: "d478c60"
  template:
    metadata:
      labels:
        app: processor-service
        preview: "d478c60"
    spec:
      containers:
        - name: processor-service
          image: ibdevacrpuj79a.azurecr.io/processor-service:preview-d478c60
          ports:
            - containerPort: 3000
          envFrom:
            - secretRef:
                name: previews-nextjsportfolio-env
          env:
            - name: NODE_ENV
              value: "preview"
          resources:
            requests:
              memory: "128Mi"
              cpu: "50m"
            limits:
              memory: "256Mi"
              cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: processor-service
  namespace: previews
  labels:
    preview: "d478c60"
spec:
  selector:
    app: processor-service
    preview: "d478c60"
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
